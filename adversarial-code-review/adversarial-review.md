# Adversarial Review

> Required input: target file / PR / spec path / code snippet must replace $ARGUMENTS before execution.
> If $ARGUMENTS is empty or missing, ask the user what to review before proceeding.

Execute the adversarial-code-review skill to review the following.

Apply the full §0-§9 workflow from the skill definition. Do NOT skip any phase.

If you have repo access, use grep to verify any function/file you reference exists before citing it.

## Review Target

$ARGUMENTS
